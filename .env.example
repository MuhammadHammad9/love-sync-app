# LoveSync Environment Variables

# Supabase (Required for Auth, Database, Realtime)
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Gemini API (Required for AI Contextual Poet & Questions)
VITE_GEMINI_API_KEY=your_gemini_api_key

# Mapbox (Required for Invisible String / Maps)
VITE_MAPBOX_ACCESS_TOKEN=your_mapbox_access_token

# Spotify (Required for Telepathic DJ)
# Note: Sensitive secrets should ideally be in Supabase Edge Functions, 
# but Client ID is needed for frontend auth.
VITE_SPOTIFY_CLIENT_ID=your_spotify_client_id
