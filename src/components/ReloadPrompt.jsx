import React from 'react';

// PWA features disabled due to build environment issues (invalid path characters)
// This component is now a placeholder.
const ReloadPrompt = () => {
    return null;
}

export default ReloadPrompt;
